# Скопируй в web/.env и заполни:  cp web/.env.example web/.env

# ngrok tunnel (npm run tunnel / npm run demo)
NGROK_AUTHTOKEN=
# NGROK_DOMAIN=your-static.ngrok-free.dev   # необязательно: стабильный домен
PORT=4173
